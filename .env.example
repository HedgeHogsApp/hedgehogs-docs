# hedgehogs-docs — environment variables
# Copy to `.env.local` if needed. The docs site is static content; most runs
# need nothing here.

# Local dev origin (CSP/dev niceties).
NEXT_PUBLIC_APP_URL=http://localhost:3022